# 「想吃啥」产品需求文档（PRD）

> **版本**：v2.1（Day 4 定稿）
> **作者**：黄闰南
> **依据**：本仓库的 `research.md`（Day 3 需求研究）
> **本文不涉及**：技术选型（属 Day 5）、任何代码实现

## 版本历史

| 版本 | 改了什么 |
|---|---|
| v1.0 | 初稿：3 个页面，21 条验收标准 |
| v1.1 | 改回三项（可选过滤、拒绝记录与长期学习、历史记录页）→ 4 个页面，41 条 |
| v1.2 | 第一轮自检后：新增 5.1 数据模型；机制由"加权随机"改为"加权洗牌袋"；新增「不想吃」按钮 → 49 条 |
| v1.3 | 第二轮自检后：定义"一轮"的唯一口径；明确装袋条件与重装时序；修正取样窗口 → 58 条 |
| v1.4 | 第三轮自检后：改为装袋时错开排布、名额加上限、写死加载口径 → 63 条 |
| **v2.1** | **第五轮自检后：修掉 3 条「必须修改」（边界衔接漏情形、E4 构造失效、J3 状态不可达），并按"缩减审查面"原则把 C/D/E/G/H/J/K 七组同类断言合并** → 57 → 41 条 |
| **v2.0** | **第四轮自检后做出方向性决定：砍掉"加权洗牌袋"，退回最简单的形态。** 去掉"名额加权"与「不想吃」按钮，只保留**洗牌袋（一轮内每样恰好一次）+ 不连续重复**。63 → 57 条，但**边界组合从数千种降到几乎为零** |

> **为什么有 v2.0**：四轮自检的问题数走出 **6 → 5 → 2 → 4** 的轨迹——第三轮降到 2，第四轮又回到 4。
> 这不是"快修完了"，而是**复杂度超过收益**的信号。根因是"加权洗牌袋"自身状态组合太多
> （名额、上限调整、错开排布、跨轮衔接、装袋失效、加载口径）。而它本来只是**为了让验收"可测"**才被设计出来的。
> **结论：一个自己都测不准的功能，比没有这个功能更糟。** 于是砍掉它。

---

## 1. 一句话定位

> **「想吃啥」是一个网页小工具，帮大学生解决"中午不知道吃什么"。**
> 打开就给你一个具体建议；用几次之后，它沉淀成**你自己认可的那份短清单**——
> 只在你自己的范围里挑，**不会连续推同一个**，也不会老是围着一两样打转。

**它不是外卖 App。** 它不点餐、不配送、不比价、不替代任何平台。
它只处理**你打开外卖 App 之前的那一刻**——那种"我根本不知道想吃什么"的卡顿。

---

## 2. 目标用户与场景

### 主要用户

在校大学生。住在宿舍或校区周边，午饭主要靠外卖或周边小店解决，中午时间紧。

### 核心场景

> 中午 11:45–12:00，下课往宿舍走的路上。饿了，但上午的课刚把脑子耗掉一半。
> 打开外卖 App，滑了二十几家店、翻了几百条评价，最后点了一份吃过的——四十分钟没了。

**用户此刻的状态**：累、饿、不想做决定。

### 次要场景

晚自习前（同样有明确的时间压力）。

### 本产品**不服务**的场景

多人聚餐、谁买单这类**群体决策**；想吃特定菜系、按预算筛选这类**有明确目标的浏览行为**；
优惠计算、凑满减（**算账问题**）。

### 用户不需要做的事（关键约束）

**不要求用户注册、登录、填写任何强制性的偏好表格。** 打开就能用——**所有设置项一律可选、默认关闭（或不填）。**

---

## 3. 问题与依据

完整论证见 `research.md`，这里只摘结论。

**问题的本质不是"懒"，是决策疲劳。** 华东师范大学《大众心理学》2025 年的一篇文章指出：
连续做决定会消耗心理资源，到后面大脑进入"低电量模式"。三个原因叠加：

1. **选项在膨胀**——几十家店、上千条评价全推到你眼前
2. **信息在过载**——点任何一家之前都能先看到几十条差评，于是**每个选项看起来都像在踩雷**
3. **容错率在降低**——一顿饭三十块，难吃＝钱白花＋中午没吃好＋下午没劲

一句话概括这种荒诞：**你在用一个远超问题本身量级的焦虑，处理一个低风险的选择。**

| 同类产品的关键教训 | 来自 | 对本产品的影响 |
|---|---|---|
| **纯随机是错的**：「转到不想吃的 → 重转 → 又变成另一种纠结」 | 「转盘决定吃什么」（评分仅 3.9） | 随机只能当**手段**，不能当核心机制 |
| **用户留下来的其实是"自定义列表"**：自己录 20–30 个认可的选项，决策时间掉到 30 秒以内 | SpinEats（作者实测两个月） | 核心是**候选池**，不是转盘 |

大学生这个群体：点餐高峰集中在**中午下课后**与**晚自习前**，**时间压力显著**（样本量 655 的调研）。
**这决定了本产品必须在"最累、最饿、最没耐心的那一刻"依然好用**——也就是必须零门槛。

---

## 4. 本期目标

**一句话**：让用户从打开页面到拿到一个"愿意接受"的建议，**不超过 10 秒，且不需要任何输入**。

| 目标 | 衡量方式 |
|---|---|
| 零门槛 | 首次打开，不注册、不填表、不做任何设置，直接看到结果 |
| 快 | 打开到出现结果 ≤ 3 秒 |
| 不烦人 | 连续两次推荐结果不重复 |
| **不扎堆** | 一轮之内，**每个条目恰好出现一次** |

---

## 5. 功能范围（MVP：4 个页面）

### 5.1 数据模型

| 字段 | 取值 | 默认值 | 说明 |
|---|---|---|---|
| `名称` | 文本，**1–20 字**（去首尾空格后按字符计） | 必填 | 去空格**包括全角空格**（U+3000） |
| `类型` | `菜` / `店` | 必填 | 二选一 |
| `辣度` | `不辣` / `微辣` / `中辣` / `特辣` / `不限` | **`不限`** | `不限` 在过滤时**视为通过任何辣度上限** |
| `忌口标签` | 文本数组，可为空 | **空** | 如"香菜""牛肉""海鲜" |

**内置库 vs 用户条目**：内置库条目的 `辣度` 与 `忌口标签` **已预先标注**；
用户自己添加的条目两个字段**可选填**，不填即 `不限` / 空数组——
**用户自己加的东西默认永远不会被过滤掉**，这保证"零门槛"不被破坏。

### 页面 1 · 首页（一键出结果）

- 打开页面，**立刻显示一个具体的推荐结果**
- 结果旁显示「类型 · 辣度」
- 一个大的 **「换一个」** 按钮：点击后立刻给出下一个结果，且与上一个不同
- 底部一行小字入口：「我常吃的那几家也加进去」→ 跳到页面 3
- **不需要任何输入、任何设置即可使用**

### 页面 2 · 我的清单

- 列出候选池全部条目，每条显示：名称、类型、辣度、忌口标签
- 每条可删除
- 显示计数并区分来源：**「共 N 个 · 其中你自己加了 M 个」**
- **可选过滤**（默认不生效）：辣度上限（默认 `不限`）、忌口标签多选（预设 + **可自己输入**，默认空）
- **「恢复默认库」**按钮：把内置库条目补进清单（已有的不重复添加）
- 候选池为空时给出引导，而不是空白

### 页面 3 · 添加

- 必填：名称（1–20 字）+ 类型（`菜` / `店`）
- **可选填**：辣度（下拉，默认 `不限`）、忌口标签（预设 + 可自己输入，默认空）
- 判重：**先去首尾空格（含全角），再去重**；同名时**不新增**，提示「清单里已经有这个了」
- 保存后立刻进入候选池，可返回首页或清单

### 5.3 数据与降级

- 本产品的全部数据（清单条目、过滤设置、历史记录）保存在**浏览器本地存储**中。
- **若本地存储不可用**（被禁用、或写入失败）：页面给出**一次明确提示**（如「数据将无法保存」），
  并**降级为内存态**——本次会话内核心功能（出结果、换一个、增删条目）仍可正常使用，
  只是刷新或关闭页面后不会保留。

### 页面 4 · 历史记录

- 按时间**倒序**列出最近推荐过的结果（最新在最上面），每条显示**名称与时间**
- **最多保留 20 条**，超出后最早的被移除
- 没有记录时显示「还没有记录」，而不是空白
- **删除条目不影响历史记录**——历史是"发生过的事"

**说明（为什么支持"菜"和"店"）**：默认装一批**菜品**（零成本起步），用户也可加常去的**店**。
研究显示目前没有产品同时解决"吃什么"和"去哪家"——两头都接上是本期的差异化点。
> 注：本产品的"内置库"是**预先标注好的菜品清单**，不是餐厅数据库（见 7.2）。

**说明（为什么要有历史记录）**：它直接回应 C 类痛点——**"吃腻了"**。

### 5.2 推荐机制（洗牌袋）

> **v2.0 重写**。上一版用"加权洗牌袋"（按拒绝次数分配名额），四轮自检暴露的边界问题几乎全部出在它身上。
> 本版砍掉加权，只保留**洗牌袋**——规则简单到**没有边界情况可钻**。

#### 术语

- **一轮**：袋中**每个条目恰好出现一次**，按顺序逐个取出。**一轮长度 = 当前候选条目数。**
- **候选条目**：通过过滤条件后剩下的条目。
- **上一次结果**：历史记录里最新的一条（含刷新前产生的）。

#### 抽取规则

1. **装袋**：把当前候选条目**随机打乱**，形成袋。
2. **取袋**：按顺序取出下一个条目，作为结果，并写入历史记录。
3. **袋取空** → 按当前候选**重新装袋**，下一次抽取从新袋开始。

#### "不连续重复"（结构性保证，无需例外）

- **轮内**：因为每个条目在一轮里只出现一次，**相邻两个结果天然不同**。
- **边界衔接（覆盖所有重装情形）**：**任何一次装袋之后**——不论是袋取空后重装、
  因候选或过滤条件变化而中途重装、还是页面加载/刷新时装袋——
  若新袋的**第一个**条目恰好等于**上一次抽取产生的结果**（含刷新前产生的、含被丢弃那一轮产生的），
  则把第一个与**第二个**交换位置。
  因候选 ≥ 2 时第二个必然与之不同，**交换一定可行，且交换后仍相邻不同**。
  > v2.0 初稿只写了"跨轮"，漏掉了"一轮抽到一半就重装"的情形（例如抽出 a 之后删掉或新增一个条目），
  > 那种情况下新袋首项可能又是 a，造成相邻重复。本条即为此修复。

> 这是 v2.0 相比 v1.4 最大的简化：v1.4 的"跨轮衔接"只证明了"交换可行"、没证明"交换后仍不相邻"，
> 因此存在反例。而在"每样只有一个名额"的前提下，**这个问题根本不存在**。

#### 袋子的失效与重装

- **装袋条件** = 当前候选条目集合（即"过滤后的条目集合"）。
- **每次抽取之前**先校验：装袋条件与当前状态**不一致 → 立即丢弃当前袋并重新装袋**；一致 → 沿用。
- **刷新或重新打开页面** → 重新装袋，并**立即取出第一个**作为显示结果、写入历史记录。
  （也就是说：**页面一出现，就已经消费了本轮的第 1 个名额。**）
- ⚠️ 上述"自动抽取"**只在「首页」加载/刷新时发生**。进入「我的清单」「添加」「历史记录」页面时
  **不会**自动抽取、也不会写入历史记录。

#### 特殊情况（在装袋之前判定）

- **候选为 0** → **不抽取**，提示「条件太严了，要不要放宽」，**不得回退到被过滤掉的条目**。
- **候选恰好为 1** → 允许与该条目的上一次结果相同，并提示「现在只有 1 个可选」。

---

## 6. 验收标准

> **读法**：每一条都应当能被**照着测一遍**，并明确给出「过」或「没过」。

**公共约定（默认适用）：**

| 约定 | 内容 |
|---|---|
| 环境 | 桌面版 Chrome，缩放 100%，本地打开 |
| **全新状态** | 清除本站浏览器存储（DevTools → Application → Clear site data），并刷新页面 |
| **恢复默认库** | 进入「我的清单」页，点击「恢复默认库」 |
| **抽取一次** | 点击一次「换一个」，并记录新出现的结果 |
| **一整轮** | **从页面加载/刷新后开始，记录连续 N 个推荐结果，其中 N = 当前候选条目数**；<br>做法：加载时自动产生的第 1 个也要记，然后点「换一个」**N−1 次**，共 N 个结果 |
| **取样窗口** | 凡"要求某内容出现"或"要求某内容不出现"的断言，一律以**记录一整轮（N 个结果）**为窗口 |
| 适用范围 | 上述约定**只适用于推荐结果**；G 组（页面上不存在某功能）这类静态断言不适用 |

### A. 零门槛（首次使用）

| 编号 | 验收标准 |
|---|---|
| **A1** | 从"全新状态"打开首页，**不出现任何登录/注册/表单**，直接可见一个推荐结果 |
| **A2** | 从页面开始加载到推荐结果显示，时间 **≤ 3 秒**（用秒表计时，清除站点数据后测一次、正常刷新后再测一次，取较慢的一次） |
| **A3** | 推荐结果是一个**具体的词**（如"麻辣烫"，或店名"沙县小吃"），不出现"随便""看心情""都行"这类空话 |

### B. 核心动作

| 编号 | 验收标准 |
|---|---|
| **B1** | 点击「换一个」，**1 秒内**出现新结果（用秒表或 `performance.now()` 从点击到结果文本变化计时） |
| **B2** | 新结果与**上一次**的结果不同（例外：候选恰好剩 1 个） |
| **B3** | **候选 ≥ 2 时**，连续抽取 **10 次**，不出现连续两次相同的结果 |
| **B4** | **构造方式**：把清单删到只剩 1 个条目。此时抽取**不报错**，结果始终是该条目，并提示「现在只有 1 个可选」 |
| **B5** | **跨轮正常**：从加载后开始，点「换一个」共 **2N−1** 次（连续记录 2N 个结果，N = 候选条目数），**全程不报错**；**每一轮内每个条目恰好出现 1 次**；**相邻两个结果不相同** |

### C. 清单管理（页面 2）

| 编号 | 验收标准 |
|---|---|
| **C1** | 条目数量与来源计数都正确：页面显示「共 N 个 · 其中你自己加了 M 个」，且 **N 与实际条目总数一致、M 与实际用户添加数一致** |
| **C2** | 删除某一条后，记录**一整轮**，该条**不出现**在结果中 |
| **C3** | 删除后条目数量**立即更新**（不需刷新）；点击「恢复默认库」后内置条目被补回，**已在清单中的条目不重复添加** |
| **C4** | 每条条目上都**能看到类型、辣度、忌口标签三个字段**，且与录入时一致 |
### D. 添加（页面 3）

| 编号 | 验收标准 |
|---|---|
| **D1** | **表单基础校验**：名称留空或只输入空格（含全角）→ 提示且不写入；类型未选 → 不允许保存并提示 |
| **D2** | **长度限制**：输入 21 个字符的名称时不允许保存并提示；恰好 20 个字符时允许保存 |
| **D3** | **去空格与判重**：名称首尾空格（半角或全角）被自动去除；与已有条目**去空格后同名**时不新增，提示「清单里已经有这个了」 |
| **D4** | **写入生效**：添加成功后清单数量 +1；把清单删到只剩 1 个条目后再添加一个（共 2 个），刷新页面后记录连续 2 个结果（加载产生第 1 个 + 点 1 次「换一个」）——**新加的条目必然出现** |
| **D5** | **不破版**：名称包含 emoji 或 `<script>` 之类的字符时能正常保存，且各处**原样显示为文本，不执行、不破版** |
### E. 数据持久化与降级

| 编号 | 验收标准 |
|---|---|
| **E1** | **刷新不丢**：刷新页面后，用户自己添加的条目仍然存在；刷新之前产生的历史记录全部保留、相对顺序不变（刷新本身会新增 1 条，属正常） |
| **E2** | **重开不丢**：关闭浏览器再重新打开，清单内容（条目、辣度、忌口标签）与关闭前一致 |
| **E3** | **存储不可用时的降级**：在**无痕/隐私窗口**中打开页面，不出现报错，核心功能（出结果、换一个）可正常使用；并以**代码审查**确认存在"提示一次 + 降级为内存态"的分支（对应 5.3 节） |
### F. 边界与容错

| 编号 | 验收标准 |
|---|---|
| **F1** | 候选池为空时，**首页与「我的清单」页**都**不出现空白页、不报错**，而是显示引导（去添加 / 恢复默认库） |
| **F2** | 四个页面在 **375px 宽**（DevTools 手机模拟）下均可正常使用：**无横向滚动条**，主要按钮可点击 |
| **F3** | **固定操作路径**：清除存储 → 打开首页 → 抽取 3 次 → 分别进入「我的清单」「添加」「历史记录」各停留 3 秒 → 回首页。全程打开 DevTools → Console，**不出现红色报错**（favicon 等资源 404 不计入） |
| **F4** | 同时打开两个标签页并各自操作，**不出现报错或白屏**；数据以最后一次写入为准（本期不做多标签同步，属已知限制） |

### G. "本期不做"确实没做（反向验证 · 静态断言）

| 编号 | 验收标准 |
|---|---|
| **G1** | 页面中**不存在**登录/注册入口，也**不存在**价格、优惠券、满减、下单按钮 |
| **G2** | 页面中**不存在**按用餐时段筛选的开关或自动规则，也**不存在**"地域/地区"筛选项 |
| **G3** | 页面中**不存在**分享、生成图片、转发入口，也**不存在**任何"多个人一起决定"的功能 |
### H. 可选的忌口 / 辣度过滤（页面 2）

> **测试前提**：先用「添加」新建一个辣度为 `特辣` 的条目、一个忌口标签含"香菜"的条目，
> 或确认内置库中已存在这样的条目；并确认候选中**至少还有 2 个**不被过滤的条目。

| 编号 | 验收标准 |
|---|---|
| **H1** | **默认不生效**：处于"全新状态"时辣度上限为 `不限`、忌口标签为空，记录**一整轮**，判定：**每一条候选都出现过一次** |
| **H2** | **过滤生效与恢复**：把辣度上限设为 `不辣`（或勾选忌口"香菜"）后，记录**一整轮**，结果中**不出现**被排除的条目（`不限` 视为通过任何上限）；**关闭**过滤后，记录**一整轮**，之前被排除的条目**出现了至少一次** |
| **H3** | **过滤为空**：清单里只留 1 个 `特辣` 条目并把上限设为 `不辣`。此时抽取**不报错、不出现任何结果**，提示「条件太严了，要不要放宽」，**不得回退到被过滤掉的条目** |
| **H4** | **条件持久化**：过滤条件在刷新页面后仍然生效 |
| **H5** | **用户条目与自定义标签**：用户自己添加、未填标签的条目（`不限` / 空）在开启任意过滤后**仍能被抽到**；在忌口标签里**自己输入**一个预设中没有的标签（如"折耳根"）后，该标签出现在过滤选项里，勾选后按 H2 的规则生效 |
### I. 洗牌袋机制（5.2 节）

| 编号 | 验收标准 |
|---|---|
| **I1** | **一轮完整性**：从一次刷新后开始，记录**连续 N 个结果**（N = 候选条目数，做法：记录加载时产生的第 1 个，再点「换一个」N−1 次）→ 判定：**每个候选条目恰好出现 1 次**，无遗漏、无重复 |
| **I2** | **候选项变化立即生效（删除）**：记录一整轮后，删除其中 1 个条目，**不刷新页面**，再记录一整轮 → 判定：**被删条目不出现，其余每样恰好 1 次** |
| **I3** | **候选项变化立即生效（新增）**：记录一整轮后，新增 1 个条目，**不刷新页面**，再记录一整轮 → 判定：**新条目出现 1 次，总数比上一轮多 1** |
| **I4** | **过滤条件变化立即生效**：开启辣度上限（会排除若干条目）后，**不刷新页面**，记录一整轮 → 判定：**结果数量等于过滤后的候选数，且被排除的条目都不出现** |

### J. 界面显示与历史记录

| 编号 | 验收标准 |
|---|---|
| **J1** | 首页的推荐结果旁显示「类型 · 辣度」，且与该条目的实际字段一致 |
| **J2** | **上限与顺序**：累计产生 21 个结果后，历史页**只显示 20 条**、**最早那一条已被移除**；且每产生一个新结果，它出现在列表**最上方**，原有记录相对顺序不变 |
| **J3** | **空历史**：清除本站存储后，**不经过首页刷新**直接进入「历史记录」页，**不显示空白页**，而是显示「还没有记录」之类的提示 |
| **J4** | **每条含名称与时间**；且把历史里出现过的某个条目从清单中**删除**后，该条**仍留在历史记录中** |
### K. 页面跳转

| 编号 | 验收标准 |
|---|---|
| **K1** | 首页底部「我常吃的那几家也加进去」**跳转到「添加」页**；在「添加」页保存成功后，**能到达首页、也能到达「我的清单」页**（两条路径都通） |

**合计 41 条**（A3 + B5 + C4 + D5 + E3 + F4 + G3 + H5 + I4 + J4 + K1）。
---

## 7. 本期不做

### 7.1 今天砍掉的功能（含砍掉的理由）

> 今天的掌握问题是「你在 PRD 里砍掉了什么功能？为什么砍它？」——这一节就是答案。

#### ① 确认砍掉（从未反悔）

| 砍掉的功能 | 为什么砍 |
|---|---|
| **按时段自动过滤** | 同类「今天吃什么」有，但要维护五套时段规则，还会缩小候选池。**本期只验证一件事：一键出结果到底解不解决问题。** |
| **地域偏好过滤** | 内置库是我们自己挑选的菜品，地域维度意义不大；"我常去的店"这一头已由"用户自己添加店"覆盖。**同一个目的，不需要两个入口。** |
| **分享卡片 / 社交分享** | 核心场景是"一个人中午吃啥"，分享解决不了任何人的痛；且要做图片生成，工期风险高。 |
| **多人决策（"和谁吃"）** | 那是另一个产品的问题（MealWheel 专门在做）。 |

#### ② 曾被我砍掉、现已改回本期做（含为什么原来砍错了）

| 功能 | 我原来的理由 | 为什么改回来 |
|---|---|---|
| **忌口 / 辣度过滤** | "要用户先配置，与零门槛冲突" | **理由只对了一半**。冲突只发生在"必须先配置才能用"的情况；做成**默认不生效的可选设置**就不冲突。 |
| **历史记录** | "超出 3 页面的范围" | **这条理由不成立**：为了"不重复"，上一个结果本来就必须存。数据已有，多做一个页面几乎不增成本。 |

#### ③ **v2.0 新砍：拒绝记录与"长期学习"**（今天最值得讲的一条）

| 砍掉的 | 为什么砍 |
|---|---|
| **「不想吃」按钮 + `拒绝次数` 字段 + 按拒绝次数分配名额的"加权洗牌袋"** | 这条功能是**改了两次又砍回去**的：v1.0 我判断"一周验证不了"砍了 → 你舍不得，v1.1 改回来 → 为让它可验收，我设计了"加权洗牌袋" → **四轮自检的问题数 6 → 5 → 2 → 4，全部出在这套机制上**（名额、上限调整、错开排布、跨轮衔接、装袋失效、加载口径）。<br>**砍它的真正理由不是"没时间"，而是：它的复杂度已经超过它能带来的价值。** 一个**自己都测不准**的功能，比没有这个功能更糟——它会让 Day 7 反复出 bug，最后连"能出结果"这个最基本的承诺都保不住。<br>**保留的部分**：用户想要的"别老是那几样"由**洗牌袋**满足（一轮内每样只出现一次）——**用最简单的机制拿到了 80% 的价值**。 |

### 7.2 沿用 `research.md` 的判断

| 不做 | 理由 |
|---|---|
| 优惠比价 / 红包计算 | 券的数据在外卖平台手里，拿不到；靠用户手动录入必死 |
| 接真实外卖平台数据 | 没有公开接口，也涉及合规问题 |
| 账号体系 / 登录注册 | 一周做不完；且"免登录"本身就是加分项 |
| 真实下单、跳转支付 | 本期根本不碰交易环节 |
| 餐厅爬虫 / 自建**餐厅数据库** | 维护成本远超一周工期。（注：我们的"内置库"是**预先标注的菜品清单**，不是餐厅库。） |
| 做手机 App | Day 7 只做**网页** MVP |

---

## 8. 待定问题（明确留给 Day 5）

1. **用什么技术做**：纯静态 HTML/CSS/JS，还是引入框架？
2. **数据存在哪里**：浏览器本地存储是否够用？（E1–E4 只要求"刷新与重开浏览器不丢"，不要求跨设备）
3. **内置菜品库从哪来、放多少条**：多少条才既不显得空、又不至于选不出来？每条要标好辣度与忌口标签。
4. **4 个页面的路由方式**：多文件还是单页切换？
5. **多标签页的数据一致性**：本期只要求"不报错"（F4），是否更严格留给 Day 5。

### 已知风险

- ✅ **机制风险已消除**：v2.0 的洗牌袋规则简单到没有边界情况可钻。
  "相邻不同"和"每样恰好一次"都是**结构性成立**的，不依赖任何额外的补救规则。
- ⚠️ **工期风险**：**4 个页面 + 41 条验收标准**。相比 v1.4（两套机制 + 63 条）明显下降，但仍不宽裕。
- **降级线**：如果一周不够，按清单要求——**首页、详情（我的清单）、一项写入功能**这三块先做扎实，其余列后续。
  **降级时必须整块砍功能并同步删掉对应验收标准**，不要半吊子。

---

## 附：这份文档是怎么来的

| 部分 | 谁做的 |
|---|---|
| 第 3 节的依据、第 7.2 节的判断 | 来自 `research.md`，由 AI 检索整理、我本人确认结论 |
| 第 1、2、4 节的文字 | AI 起草，**我逐条过目** |
| 第 5 节（含 5.1 数据模型、5.2 推荐机制）、第 6 节验收标准 | AI 起草，**我逐条过目** |
| 第 7.1 节"砍掉什么、为什么砍" | AI 列候选并给出理由，**由我确认**——这是今天的掌握点 |
| **v1.1 改回三项** | **由我提出——我不同意 v1.0 的砍法**，AI 说明了保留的代价，我确认保留 |
| **v2.0 砍掉"长期学习"** | 四轮自检证明这套机制复杂度失控；**我确认砍掉**，退回最简单的洗牌袋 |

### 附：AI 自检记录（四轮）

**方式**：每轮都由**全新的独立 AI 实例**完成，只拿到本 PRD 与 `research.md`，唯一任务是挑毛病；
并**明确要求它不要相信文档里"已修复"的自述，必须自己验证**。

| 轮次 | 针对版本 | 结论 | 主要问题 |
|---|---|---|---|
| 第一轮 | v1.1 | **未通过**（6 条必须修改） | 标签体系缺失；"必然出现"与随机机制冲突；测试步骤无法构造；凭空加规则；前后矛盾；过滤为空时回退违反其它标准 |
| 第二轮 | v1.2 | **未通过**（5 条） | "一轮"有互斥定义；装袋时序两种读法并存；袋的边界不可观测；公共约定的取样窗口不成立；交换规则在袋尾无定义 |
| 第三轮 | v1.3 | **未通过**（2 条） | ①加载口径未定义 → I 组假失败率 53%–64%；②兜底分支截断本轮（触发率 20–24%）。<br>该轮审查还**自行做了 4 万次模拟**来支撑结论 |
| 第四轮 | v1.4 | **未通过**（4 条） | 跨轮衔接只有"交换可行"、无"交换后仍不相邻"的证明（给出反例）；另有 3 条因输出截断未取得。<br>同时**独立确认**：名额公式与上限调整自洽且有证明；I 组 N 值逐条核算全部正确 |
| — | **v2.0** | 见提交前记录 | 针对第四轮结论，**做出方向性决定：砍掉加权机制**，退回最简单的洗牌袋 |

**四轮问题数轨迹：6 → 5 → 2 → 4。** 第三轮降到 2、第四轮又回到 4，是本次决定砍掉机制的**直接依据**。
